export enum USER_ROLES {
  ADMIN = 'admin',
  APPLICANT = 'applicant',
  RECRUITER = 'recruiter',
  GUEST = 'guest',
}

export enum USER_STATUS {
  ACTIVE = 'active',
  RESTRICTED = 'restricted',
  DELETED = 'deleted',
}
